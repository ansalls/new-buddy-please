# <Project Name>

<!-- Tagline: a one-sentence elevator pitch. Optional but recommended. Example: -->
<!-- > A persistent HTTP wrapper around a local agent backend CLI for scripted triggers. -->
> One-sentence elevator pitch.

## Summary

What this project is, who it's for, and why it exists. Keep the first paragraph
self-contained — the Tomes overview surfaces it as the project preview.

Optional second paragraph for context, history, or scope. Plain prose only:
no headings, code blocks, images, or tables in this section.

## Capabilities

<!-- 3–8 flat bullets. For tool-heavy repos (8+ entry points) keep these as     -->
<!-- high-level themes and list the full inventory under ## Tools below.        -->
<!-- For content/curation repos, list collection or category names instead of   -->
<!-- code features (e.g. "**Code review prompts** — patterns for PR review.").  -->
- **<Capability>** — <one-line description of what it does today>.
- **<Capability>** — <one-line description>.
- **<Capability>** — <one-line description>.

## Status

<!-- Optional. One line on maturity (planning / alpha / beta / stable / maintenance / -->
<!-- deprecated) plus any platform or runtime constraint. Use ## Roadmap for          -->
<!-- progress tracking; this section is for maturity, not what's done so far.         -->
Alpha. Linux + macOS supported; Windows untested.

## Prerequisites

<!-- Optional. External dependencies, accounts, hardware, or platform requirements -->
<!-- that must be in place before Setup makes sense. Delete if not applicable.     -->

## Setup

Concrete install/build steps:

```bash
# example
```

## Usage

Smallest useful example. Link to fuller docs rather than inlining a manual.

## Tools

<!-- Optional. Use this section (or `## HTTP API` / `## Subcommands`) when the project -->
<!-- exposes many discrete entry points. Delete the section if it doesn't apply.       -->

| Tool   | Description       |
| ------ | ----------------- |
| `foo`  | What `foo` does.  |
| `bar`  | What `bar` does.  |

## Configuration

<!-- Optional. Env vars, config-file keys, and persistent runtime flags. Distinct  -->
<!-- from Setup (install steps) and Usage (per-run invocation). Delete if your     -->
<!-- repo has only a couple of env vars — they fit fine in Setup in that case.    -->

## Development

How to run tests, build locally, and contribute changes.

## Roadmap

<!-- Optional. What's done, in progress, and planned. Tables and progress checklists -->
<!-- belong here — not in ## Status or ## Capabilities.                              -->

## License

Project license, if applicable.

## Acknowledgements

<!-- Optional. Attribution for upstream projects, prior art, or vendored libraries. -->
<!-- ## Credits is an accepted alternative heading.                                 -->

## See Also

<!-- Optional. Alternative implementations, sister repos, or projects this composes -->
<!-- with. ## Related Projects is an accepted alternative heading.                  -->
